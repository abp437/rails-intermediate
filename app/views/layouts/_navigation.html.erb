<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container">
    <%= link_to 'Collabfield', root_path, class: 'navbar-brand' %>
    <div class="collapse navbar-collapse" id="navbar-collapsible-content">
      <ul class="nav navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a id="user-settings" class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false" href="#">
              <span id="user-name"><%= current_user.name %></span>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
            </ul>
          </li>
        <% else # user not signed it %>
          <li ><%= link_to 'Login', login_path, class: 'ms-3' %></li>
          <li ><%= link_to 'Signup', signup_path, class: 'ms-3' %></li>
        <% end # if user is signed it %>
      </ul>
    </div><!-- navbar-collapse -->
  </div>
</nav>

<!-- Collect the nav links, forms, and other content for toggling -->
